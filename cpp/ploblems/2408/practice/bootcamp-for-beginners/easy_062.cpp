#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
    int H, W;
    cin >> H >> W;
    string C;
    rep(i, H) {
        cin >> C;
        cout << C << endl << C << endl;
    }
    return 0;
}
