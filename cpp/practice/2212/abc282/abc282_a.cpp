#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
    int K; cin >> K;
    for (int i=65; i<65+K; i++) {
        cout << char(i);
    }
    return 0;
}
