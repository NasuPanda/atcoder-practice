#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
    int N; cin>>N;
    int P[N],Q[N]; rep(i,N) {cin>>P[i];} rep(i,N) {cin>>Q[i];}
    
    return 0;
}
